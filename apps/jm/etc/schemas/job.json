{
  "$schema": "http://json-schema.org/schema#",
  "id": "http://jm.forma-pro.com/schemas/job.json",
  "allOf": [
    { "$ref": "jobPattern.json" },
    {
      "type": "object",
      "properties": {
        "status": { "type" : "string", "enum": [ "new", "running", "canceled", "completed", "failed", "terminated" ] }
      }
    }
  ]
}