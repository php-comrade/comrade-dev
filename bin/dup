#!/usr/bin/env bash

docker-compose up -d
trap "docker-compose stop jm_consumer; killall fswatch" SIGTERM  SIGINT

fswatch --latency=0.5 -o -L apps/jm                   | xargs -n1 -I{} docker-compose restart jm_consumer &

docker-compose logs --tail=10 -f jm jm_consumer
